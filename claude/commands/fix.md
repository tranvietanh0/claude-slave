---
description: Quick fix - analyze error and fix it immediately
---

## Context

- Current directory: !`pwd`
- Recent terminal output (check for errors): !`cat ~/.bash_history 2>/dev/null | tail -5 || echo "No history"`

## Task

User wants to fix an error. Follow these steps:

1. If user provided an error message, analyze it
2. Find the problematic file/code
3. Apply the fix directly
4. Verify the fix works

Be concise. Fix first, explain briefly after.
